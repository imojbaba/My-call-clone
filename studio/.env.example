# Copy to .env for local runs. In a Claude Code cloud session, add these as
# environment variables in the environment settings instead.

# Planning step (brief -> copy, change list, asset prompts). https://console.anthropic.com
ANTHROPIC_API_KEY=

# Image + video generation through fal.ai (one key covers every model in config/models.json).
# https://fal.ai/dashboard/keys
FAL_KEY=
